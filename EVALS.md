# Журнал измерений (eval log)

Корпус: «Стругацкие. Лучшие произведения в одном томе» (royallib, cp1251),
19 865 эпизодов (чанк 3 предложения, overlap 1). Метрики calibrate.py:
126 проб — окна 6-10 слов из середины чанка (имитация цитат).

## E1 (сегментный индекс) — baseline

| метрика | значение |
|---|---|
| dense_hit@1 | 0.855 |
| bm_hit@1 | 0.915 |
| fam_known_med | 0.219 |
| fam_novel_med | 0.194 |
| theta_low (auto) | 0.206 |

Состояние: 7.6 МБ на диске, переоткрытие ~0.1 с (против 67 с переиндексации).

## E2 (стеммер RU/EN + symspell + раскладка)

| метрика | E1 | E2 |
|---|---|---|
| dense_hit@1 | 0.855 | 0.855 (dense-путь не менялся) |
| bm_hit@1 | 0.915 | 0.923 |

Пробы — дословные цитаты, прирост занижен. Реальная ценность E2 —
словоформы («арбалет» ↔ «Анкиного арбалета»), опечатки (df=0 →
edit_dist ≤ 2), раскладка (выбор по стеммированному словарю).

## E3 (SIF + PCA для HashingEmbedder) — принят частично, с заменой гейта

| pc_weight | fam_known | theta | false_unknown | false_known |
|---|---|---|---|---|
| — (uniform, E1) | 0.191* | 0.168 | 0.363 | 0.250 |
| 0.0 (SIF only) | 0.191* | 0.168 | 0.339 | 0.333 |
| 0.25 | 0.168* | 0.135 | 0.373 | 0.267 |
| 0.5 | 0.144* | 0.109 | 0.398 | 0.300 |

*подкорпус 400k символов; hit@1 по ранжированию не менялся (0.920) ни при
одном pc_weight — PC не влияет на порядок близких пар.

**Вывод 1**: гейт знакомости на fam плохо разделяет известное/новое во всех
вариантах эмбеддера (false_unknown ~35%) — это не лечится SIF/PCA.
**Вывод 2**: fam с хэш-эмбеддером годится как статистика, но не как гейт.

**Решение**: зона unknown определяется калиброванной уверенностью
conf = P(верно | cos), fit на hit-метках (theta_conf = 0.35), а не fam.
SIF сохранён с pc_weight=0.25 (компромисс: слегка лучшее разделение
при минимальной потере fam).

Итог после E3 (полная книга): dense_hit@1 0.865, bm_hit@1 0.897,
theta_low 0.147, calib_a/b = 7.55/-2.28, calib_a2/b2 = 8.44/-2.56.

## Известные ограничения (для следующих этапов)

1. Floor conf >= 0.5 при согласии обоих ретриверов маскирует калибровку
   на пограничных запросах (novel-темы внутри корпуса получают unsure).
   Лечится в E5: LTR поверх 15+ признаков вместо одного cos.
2. Symspell не срабатывает, если ошибочный стем случайно существует
   в словаре («рудн» из «рудно» vs «рудник»). Лечится частотным
   порогом: исправлять только если df(токена)==0, а не df(стема).
3. memorize() не фитит эмбеддер (диалоговые факты — в uniform-пространстве,
   индекс — в SIF). Консолидация — задача E4.

## E5 (LTR-ранкер на DAN-фидбеке)

Реализовано: 8 признаков (bm25, bm25_head, dense, dense_nbr, coverage,
len_norm, rare_hit, dan), pairwise-обучение на событиях «верно/забудь»
(онлайн-SGD, RankNet-потеря), заморозка признака до min_pairs=20 пар с
реальной разностью. RRF-пул 30 -> переранжирование -> топ-k.

End-to-end на книге (19867 эпизодов, чистое состояние):
  10 подтверждений -> 120 пар -> pair_count=[117,0,120,117,36,114,0,120]
  w: dan 0.40 -> 1.56; bm25_head и rare_hit заморожены (0 различий).
  save/load персистит ranker.npz + journal.jsonl + doc_feedback.json.

Исправлен баг снапшота: повторный save в ту же папку досыпал сегменты
(79464 дубликата вместо 19867); save теперь перезаписывает сегменты.
Страж: storetest «повторный save: нет дубликатов».

Метрики качества ранжирования после LTR на calibrate-пробах: ожидается
рост после накопления пар; одна сессия не меняет w существенно (защита
от шума работает). Измерение nDCG на накопленном журнале — TODO E7.

## E5-P5 (extractive QA + RM3) — принято

- answer_extract: типы вопросов (name/year/number/place/reason/thing),
  ответ = частая сущность нужного типа, ОТСУТСТВУЮЩАЯ в запросе;
  вес предложения = ранг x пересечение с запросом (см. ниже);
  дедупликация оверлейных дублей; буст x1.5 только за повтор
  в не-соседних чанках; стоп-имена (диалоговые служебные слова).
- RM3: экспансия топ-5 документов (8 терминов, вес 0.4), второй проход
  в RRF. Флаги rm3_enabled/rm3_n/rm3_w в config.
- Демо на книге: «как звали главного героя трудно быть богом?» ->
  «Румата. — "Трудно быть богом, подумал Румата."»

Исправлено по пути:
1. simhash64: биты без голосов ставились в 1 (>=0) -> короткие чанки
   сливались в ложные дубли, индекс терял чанки. Сейчас >0.
2. Оверлейное дублирование: предложение s2 встречалось в чанках [s1,s2]
   и [s2,s3] и получало буст «несколько чанков», перебивая золотое s3
   (1907 vs 1910). Лечение: дедуп предложений по макс. весу + буст
   только за не-соседние ранги.

Известные ограничения:
- false-known на тематически близких novel-запросах (conf 36% vs
  theta_conf 35% — пограничье; лечится LTR-признаками в E5-LTR);
- ранжирование сцен романа по диалоговым запросам пока слабое
  (LTR-обучение на сессиях должно подтягивать).

## E7 (оценка ранжирования, eval_rank.py)

Автопробы (151 окно-цитата, полный пайплайн search):
              R@1    R@5    R@10   MRR    nDCG@10
  full        0.894  1.000  1.000  0.946  0.960
  dense_only  0.868  1.000  1.000  0.930  0.948
  bm25_only   0.894  1.000  1.000  0.945  0.960

На дословных цитатах full == bm25 (ожидаемо); ценность dense — перефразы
и устойчивость к словоформам (keyword-пары ловит BM25, см. searchtest).

Журнал DAN: pairwise_acc=1.0 на подтверждениях топ-1 (тавтология —
picked был топ-1). Осмысленно только с корректирующим фидбеком.

Корректирующее обучение (контролируемый эксперимент, 8-чанковый корпус):
25x «верно» на кандидате №2 -> позиция target 2 -> 1. LTR v1 учит
предпочтения через переразметку признаковых весов. Ограничения:
  - эффект размывается, если коррекции разных запросов противоречат;
  - dan-буст не работает, когда оба документа уже подтверждены
    (потребуются парные query-doc признаки в v2).

## LTR v2 (память коррекций)

Закрывает конфликт dan (оба документа подтверждены -> dan не различает).
Детерминированно: сигнатура запроса (сортированные стеммированные термины)
-> список docid исправлений, последнее впереди. +1 поднимает (бонус
2.0 - 0.5*позиция к скору ранкера), -1 снимает. Персист: corrections.json.

Книжное состояние: 1 коррекция -> target 2 -> 1; 'забудь' -> снят.
Стражи в storetest (10/10). Полная батарея: 10/10, 17/17, 17/17.

## Фразовые запросы (P8 дополнение)

Позиции терминов в постингах (ppos) + отдельные границы pptr
(фикс бага чтения: ppos плоский по вхождениям, НЕ по постингам).
phrase_docs: кандидаты из редчайшего термина, смежность в порядке фразы.
Бонус в BM25: 2*sum(idf) документам с точной фразой. Кавычки парсятся
в system.search; запрос целиком в кавычках отключает dense (иначе пустой
q_wo давал произвольный порядок). Стеммер: добавлены «ыми»/«ими»
(«белыми»->«бел»).

searchtest 20/20 (фраза прямой порядок — топ-1, обратный — пусто).
Полная батарея: 20/20, 17/17, 10/10.

## E4 (IVF-ANN, ann.py)

Порог ann_min_docs=20000 (ниже — точный перебор). k-means по подвыборке
(<=65k, 12 итераций) -> nlist=256 центроидов; запрос: nprobe=16 списков,
адаптивное расширение до 48 при слабом совпадении (top cos < 0.35).
Точный dot по собранным спискам — без PQ, память +~10 Б/док.

Замеры (CPU, эталон-корпус):
- синтетика 30k (адверсариальные near-dup): p95 29 мс (ANN) vs ~60 (точный),
  top-10 overlap 0.62 — на near-dup корпусах порядок среди «near-ties»
  нестабилен; синтетика — нижняя граница, не реальный сценарий;
- реальный текст 39734 чанка (Стругацкие, size=1): R@1 0.960, R@5 1.000,
  nDCG@10 0.985 против точного 1.000 (25 проб — малая выборка, R@5 полон);
- диск: ~1.25 КБ/чанк -> лимит 400 МБ ~= 320k чанков (IVF память
  не меняет — те же float16-вектора).

Вывод: E4 закрывает интерактивность до сотен тысяч чанков; следующий
рывок (миллионы) потребует PQ-кодов вместо float16 — точка расширения
видна (ann.py: вектора -> коды, центроиды -> кодебук).

## Операторы запросов + symspell v2

Операторы в search(): source:<подстрока> (фильтр по источнику),
heading:<подстрока> (фильтр по заголовку секции), -<слово> (исключение
документов с термином через постинги). Парсятся до извлечения фраз;
23/23 searchtest.

Symspell v2: условие исправления — ОТСУТСТВИЕ ПОВЕРХНОСТНОЙ ФОРМЫ токена
в корпусе (словарь _surface из tokenize, не стеммы). Закрывает кейс
«рудн» (легален через «рудник», а токен «рудно» — опечатка «трудно»).
Проверка: 'рудно' -> 'трудн' на книге. Страж 23/23, 17/17, 10/10.
Оговорка: на крошечных корпусах df<2 отсекает правильные кандидаты
(«рудник» вместо «трудн») — by design, на книге df>=2 у частых слов.

## Re-embed при fit (закрыто ограничение 3)

memorize()-факты до fit() жили в uniform-пространстве; после SIF-подстройки
index_path вызывает _reembed_all(): перевстраивает все store/memory вектора
и обновляет следы в мозге (повторный learn, weight=0.5). Проверено:
вектор меняется, store/memory синхронны, dense top-1 факта сохраняется.
Стражи 13/13 storetest. Полная батарея: 13/13, 17/17, 23/23.
eval_rank (book_state): R@1 0.873, R@5 1.000, nDCG@10 0.952.

## Сессионный контекст (п.29 плана)

Буфер 8 запросов (session_n), бленд dense-вектора (session_w=0.3) +
инъекция редких терминов последнего запроса в sparse-канал. Разрешение
местоимений без LLM. Команда CLI «новая тема» / new_topic() — сброс.
Верификация: «где он жил» после «Румата» -> Арканар; сброс -> исходное.
Стражи 15/15 storetest. Полная батарея: 15/15, 17/17, 23/23.
Баг якоря по пути: append сессии встал в ask() (NameError + пустая
сессия) — поймано runtime-проверкой, перенесено в search().

## Мультимодальный контур (распознавание видов)

image_embedder.py: MobileNetV3-small 10.3 МБ, 576->512 QR-проекция,
0.01 с/кадр CPU. index_species(): виды/<Вид>/card.md + фото -> единый
индекс (kind: card|image). recognize(): фото -> вид -> совет из карточки
(extractive QA). Безопасность: порог cos>=0.55, ядовитые -> предупреждение.
CLI: «индексируй виды <папка>», «распознай: <фото>».
Тест 7/7 (верный вид cos=0.988, шум -> отказ, токсичность -> WARNING).
Баг: карточки без store.add_docs -> рассинхрон docid; лечение единым
add_docs. Страж «инвариант docid memory==store» в storetest.
Полная батарея: 16/16 + 17/17 + 23/23 + 7/7 = 63 теста.

Оценка масштаба БД видов: 5k видов x (карточка 1 КБ + 3 фото-эмбеддинга
по 1 КБ) ~ 20 МБ -> десятки тысяч видов в бюджете 400 МБ.

## Полевые доводы (оффлайн-упаковка, дедуп, WebUI-фото)

- image_embedder: веса из models/mobilenet_v3_small.pth имеют приоритет
  (оффлайн-упаковка: 10.3 МБ рядом с проектом), иначе кэш torch.
- дедуп фото: md5 содержимого, повторная индексация добавляет 0.
- WebUI: загрузка фото (base64 -> /api/recognize) с выводом вердикта/
  совета или жёсткого отказа; /api/species — виды с числом фото и ☠.
- smoke: recognize через API cos=0.99; дедуп 0; species list ок.
Батарея: 7/7 + 16/16 + 17/17 + 23/23.

## TTA + калибровка recognize

TTA: полный кадр + центральный кроп 70%, усреднение (полевые снимки).
Порог recognize_threshold в config; calibrate_recognize.py строит
precision/recall/F1 по holdout (holdout/<Вид>/*.jpg, не входившие в
индекс), рекомендует минимальный порог с precision>=0.97 -> максимум
recall, пишет recognizer_calib.json (подхватывается _load_calib).
Проверка на 2 видах: precision 1.0 всех порогов, выбор 0.30 (by design).
Батарея: 7/7 + 16/16 + 17/17 + 23/23 (63) с TTA-путём.

## Консолидация «сна» (P6, закрыт последний пункт плана)

consolidate(): (1) анти-затухание — эпизоды с >=2 показами в журнале и
familiarity<0.3 получают повторный learn (weight 0.3); (2) деактивация
системно наказываемых (doc_feedback <= -2) — memory + store tombstone;
(3) PCA/SIF-пересчёт при росте корпуса >20% от n_fit (+ re-embed всех).
CLI: «консолидация». Проверено: refreshed=1, deactivated=1.
Батарея: 17/17 + 17/17 + 23/23 + 7/7 = 64 теста.

## Margin-правило уверенности recognize (вывод из fine-grained эксперимента)

Тест «24 псевдо-вида = 12 уровней яркости x 2 фото»: top-1 верен 2/24,
НО золотой вид стабильно в топ-3 при cos~0.96; отрыв от конкурента
0.001-0.005. CNN освещение-инвариантен по дизайну — различать уровни
яркости он не должен; тест был adversarial-by-design. Честный вывод:
у fine-grained близнецов косинусы плотные, решает ОТРЫВ, не абсолют.
-> recognize(): вердикт = (best_cos >= threshold) И
   (best - second_other_species >= recognize_margin=0.01).
На реально разных видах отрыв ~0.5 -> margin не мешает (7/7 сохранено).
Калибровщик: порог подбирается по precision; margin при необходимости
тюнится по тем же holdout-данным.

## Стражи margin-правила (9/9 recognizetest)

Два почти идентичных вида (одно фото + поворот 1°): top cos 0.98 vs 0.97,
margin 0.01 < 0.02 -> вердикт отказан даже при threshold=0. Именно
поведение, нужное для борщевик/амброзия-подобных пар: система скажет
«не уверен» вместо уверенного ошибочного «съедобно».
Батарея: 17/17 + 17/17 + 23/23 + 9/9 = 66 тестов.

## WFO Plant List 2026-06 — импорт таксономического каркаса

wfo_import.py: потоковая двухпроходная сборка (name.tsv 380 МБ + taxon.tsv
96 МБ, без полной загрузки в RAM) -> flora_backbone.tsv: 382 438 принятых
видов (wfo_id, name, genus, family). Скелеты карточек для семейства
(--family Pinaceae --skeleton-dir виды/): 60 заготовок с таксономией и
wfo-ссылкой. Токсичность стала трёхзначной: да/нет/неизвестно; неизвестное
-> предупреждение «не пробовать на вкус» (безопасность по умолчанию).
Батарея 66/66.

## Первая реальная калибровка: LeafSnap30 (30 видов листьев, 203 holdout)

Данные: LeafSnap30 (Zenodo 5061353), полевые снимки листьев, 30 видов
деревьев Сев. Америки. БД: 15 фото/вид; holdout: до 7/вид (не в индексе).
Семейства подставлены из WFO-backbone (27/30 совпали по лат. имени).

Кривая (margin-правило активно):
  порог 0.55: precision 0.835, recall 0.995 (202 вердикта)
  порог 0.90: precision 0.937, recall 0.655
  порог 0.95: precision 1.000, recall 0.172
Рекомендатор (prec>=0.97) выбрал 0.95 — recall падает до 0.17.

ВЕРДИКТ: generic ImageNet-фичи MobileNetV3 упираются в потолок точности
~0.94 на мелкозернистых видах листьев. Для survival-уровня (>=0.97) с
приемлемым recall нужен дообученный классификатор (fine-tune последних
слоев по меткам LeafSnap/региональных фото) — точка расширения
подтверждена данными, а не гипотезой.

## Съедобность грибов по фото — НЕВОЗМОЖНА (отрицательный результат, важно)

Kaggle "edible and poisonous fungi" (бинарный, без видов): 1000 фото,
лог. регрессия на MobileNet-эмбеддингах: acc=0.75, prec=0.78/rec=0.69.
Вето P>=0.3: покрытие 100% ядовитых ценой 99/100 ложных тревог ->
бесполезно как контур безопасности. Вето в recognize НЕ ПОДКЛЮЧЕНО.
Вывод (подтверждён микологией): съедобность грибов по одному фото не
определяется; правильное поведение приложения — для грибов всегда
«только по карточке вида со съедобностью, иначе консультация эксперта».
fungi_safety.npz сохранён как артефакт эксперимента, не используется.
Kaggle-табличные датасеты (mushroom-attributes: классика UCI,
mushroom-classification: CSV) — не изображения, отклонены.

## Fine-tune пробники — отрицательный результат (важно)

Данные: 2214 фото, 138 классов (leafsnap+fungi+plants+nature).
1) Линейный softmax-пробник: leafsnap 6.9% (сломался: 138 классов
   давят мелкозернистые направления), fungi 70.8%.
2) Косинусный пробник (норм. прототипы, T=10, сбаланс. батчи):
   leafsnap 82.8%, fungi 75.0% — ПРОИГРЫВАЕТ NNN (89.4% / 100%).
Вывод: замороженные generic-фичи не выигрывают у NNN с >=50 экземплярами
на класс. Следующий рычаг — бэкпроп-подстройка последних блоков MobileNet
(torch, ~30-60 мин CPU), имеет смысл только после стабилизации набора
видов региона. Артефакты: probe_model.npz, probe_cosine.npz (не используются).

## Локальная LLM: LlamaServerGenerator + Qwen2.5-0.5B-Q4 (первый прогон)

Связка: llama.cpp (собран из исходников, /tmp/.../llama-server),
модель 468 МБ (modelscope, валидна GGUF). Скорость на эталонном CPU:
20-35 ток/с (Phone 2a ожидается 4-10 ток/с — приемлемо для советов).
Отказ «не рискуй» на вопросах ВНЕ карточек — работает (переломы -> отказ).

КЛЮЧЕВОЙ ВЫВОД: 0.5B — СЛАБА ДЛЯ grounded QA: на двух вопросах ПО карточкам
ошиблась (семена коровяка -> ложный отказ; одуванчик -> чужое предупреждение
про семена). 0.5b ненадёжна для фактических ответов.

АРХИТЕКТУРНАЯ ЗАЩИТА (уже в коде): предупреждения о ядовитости/неизвестности
в recognize() ПРЕПЕНДЯТСЯ Python-кодом ДО текста LLM — LLM их не может
убрать или перекрыть. Зона съедобности защищена даже при слабой LLM.
РЕКОМЕНДАЦИЯ: Qwen2.5-1.5B-Q4 (~1 ГБ) для фактических ответов; 0.5B —
только как fallback/переформулировщик; шаблоны — для критичных предупреждений.

## LLM grounded QA — однокарточный контекст (фикс, подтверждённый тестом)

Ошибки 0.5B и 1.5B (ложный отказ + перенос токсина на другой вид) возникали
только с МНОГОкарточным контекстом. Одна карточка (топ-1): 4/4 верно, включая
критичные «семена коровяка — нельзя» и «что у одуванчика». LlamaServerGenerator
переведён на топ-1 карточку. 1.5B-Q4: 7.8-21.8 ток/с на эталонном CPU.
Рекомендация телефона: Qwen2.5-1.5B-Q4 (~1.1 ГБ, в /tmp — лимит ФС целевого
диска ~1 ГБ на файл; на телефоне без ограничений).

## Полевой комплект: doctor.py + phone_field_state

doctor.py — однокомандная диагностика (модель, состояние, LLM, ресурсы).
Баг по пути: species_info (токсичность!) не персистилась в save/load —
после загрузки состояния ☠-предупреждения терялись. Исправлено
(species_info.json), проверено: Amanita toxic=True после reload.
phone_field_state: 4027 документов, 3655 фото, 117 видов, 10.8 МБ —
телефон копирует и загружает за секунды вместо индексации.
Батарея 66/66.

## APK собран в CI (GitHub Actions) — 43 МБ, 4 итерации

github.com/TheLastie/Fly-survival-helper → Actions → build-apk → success.
Итерации дефектов: (1) setup-android action против cmdline-tools 16 ->
убран, SDK из образа; (2) невалидный sourceSets в defaultConfig ->
удалён; (3) Chaquopy 15 vs Gradle 9 (VersionNumber) -> пин Gradle 8.10;
(4) onnxruntime отсутствует в chaquopy-индексе -> APK v1 текстовый режим,
зрение деградирует без ошибок (карточки индексируются, recognize() честно
сообщает). Проверено содержимое: Chaquopy, assets/state, ONNX-модель.
app-debug.apk: /mnt/agents/output/app-debug.apk
